.container
  %h2= @board.title

  - @tasks.each do |task|
    = link_to board_task_path(@board, task) do
      .board
        .board-title
          = task.title
        .board-content 
          = task.content
        .board-user
          = task.user.email
  = link_to new_board_task_path(@board) do
    .new-board
      %p ＋Add Task