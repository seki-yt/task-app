.container
  %h2 All Boards
  .cards
    - @boards.each do |board|
      .board 
        .board-title
          = board.title
        -# .board_detail_actions
        -#   .dropdown
        -#     = image_tag 'actions.svg', class: 'dropbtn'
        -#     .dropdown-content 
        -#       = link_to '編集する', edit_board_path(board.id)
        -#       = link_to '削除する', board_path(board.id)
        .board-content 
          = board.content
        .board-user
          = board.user.email